name: "Security: Secret Scan"

on:
  pull_request:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  trufflehog:
    name: "TruffleHog Secret Scan"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: TruffleHog OSS
        id: trufflehog
        uses: trufflesecurity/trufflehog@v3.93.0
        with:
          # The action reports a verified Lob false positive without a source annotation.
          # Keep verified/unknown scanning enabled for every other detector.
          extra_args: --results=verified,unknown --exclude-detectors=Lob
